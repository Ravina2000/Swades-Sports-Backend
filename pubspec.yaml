name: quickslot_backend
description: QuickSlot REST API — concurrency-safe sports slot booking
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^2.7.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.14
